# Pending entries

<!-- Written by `propose`, read by `find` as a fallback when the master list misses, and cleared by
     `apply` — which the run offers after the brief is issued, on the user's yes. An entry here is a
     legitimate source: it has a confirmed user answer behind it. -->

_None._
